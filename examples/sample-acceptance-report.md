# 示例验收报告

> ⚠️ **本文件是结构示例，非某次真实运行的验收结果。** 所有数字、文件名、commit hash 均为占位演示，仅用于展示 `delivery-acceptance` 产出的验收报告长什么样。真实产物以你项目里 `<计划名>-acceptance-report.md` 为准。
>
> 真实运行时，每个结论都会挂真实 `commit hash` / `file:line` / 探针输出作为证据。

---

# <计划名> 验收报告（示例）

> 验收对象：`docs/plans/example-plan.md`（示例路径）
> 验收方式：计划对照 + 源码审查 + 真实测试运行 + 边界探针
> 验收日期：YYYY-MM-DD

## 一、计划完成度：100%（Task 1-9 状态）

| Task | 内容 | 提交 | 结论 | 证据 |
|------|------|------|------|------|
| 1 | 建数据结构 | `<hash1>` | ✅完成 | `git show <hash1> --stat` 改动文件与计划一致 |
| 2 | 实现 core 函数 | `<hash2>` | ✅完成 | 关键函数签名与计划一致 |
| ... | ... | ... | ... | ... |

**评审硬伤对账（A1-A3）**：逐条 ✅ + 证据。
**Self-Review 声明的"本期不做"**：声明与实现一致。

## 二、代码质量：良好（4/5）

**优点**：纯函数与 IO 分层清晰；命名跨模块一致；注释解释 why。
**扣分项**：某处校验不对称（可简化）。

| 维度 | 得分 | 理由 |
|------|------|------|
| 可读性与命名一致性 | 4/5 | ... |
| 边界与健壮性 | 3/5 | ... |
| 误报防护 | 4/5 | ... |
| 设计分层 | 5/5 | 无循环依赖 |
| 重复/冗余 | 4/5 | ... |

总体评级：良好。Top 3 改进建议：...

## 三、Bug 审计：0 个确认 bug，2 个低风险健壮性缺口

**测试基线**：`node --test tests/*.test.mjs` → 725 pass / 0 fail（示例数字）。
**探针**：写 8 组边界探针，喂计划测试未覆盖的中间态。

| 编号 | 严重度 | 位置 | 描述 | 影响 | 状态 |
|------|--------|------|------|------|------|
| B7 | 低 | `store.mjs:15` | 透传字段不校验 | 外部污染时畸形透传 | 已验证复现 |
| B8 | 低 | `check.mjs:42` | 边界输入误判 | 仅畸形输入触发 | 疑似需确认 |

每个 bug 附探针输出片段作为证据。

## 四、执行者水平评价

3-5 段，从计划执行力、TDD 纪律、诚实守规、代码品味、提升空间五方面评价。
总评：合格（示例）。

## 五、改进点清单（按 P1/P2/P3）

| # | 优先级 | 问题 | 已验证 | 详见 polish 计划 Fix |
|---|--------|------|--------|---------------------|
| 1 | P1 | B7 透传不校验 | ✅ | Fix 1 |
| 2 | P2 | B8 边界误判 | 疑似 | Fix 2 |

## 六、最终审查（修复后）

- **逐 Fix 独立验证**：Fix 1 探针结果（应校验 / 实测已校验）✅
- **全量回归**：731/731 全绿（示例数字）
- **依赖方向核查**：无循环依赖
- **子代理诚实披露核查**：无"等价修正/跳过"需核实项

## 七、可合并判定

✅ 可合并（示例）
理由：三维度通过 + 复审全绿 + 无回归。
